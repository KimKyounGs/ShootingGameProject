using UnityEngine;

public class Weather : MonoBehaviour
{
    public GameObject rain;
    void Start()
    {
        
    }

    void Update()
    {
        // posX
        // Vector2(Random.RandomRange(-2, 2))
        // Instantiate(rain, Vec, Quaternion.identity);
    }
}
